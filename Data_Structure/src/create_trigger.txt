CREATE OR REPLACE TRIGGER TRI_YY_PY_AUDITOR_YXQZ_BF BEFORE UPDATE ON YY_PY_AUDITOR FOR EACH ROW
BEGIN IF (TO_DATE(:NEW.YXQZ, 'YYYY-MM-DD') < ( TO_DATE(SYSDATE)))

THEN
 :NEW.DQZT := '过期';

ELSE
 :NEW.DQZT := '在校';

END IF;
END;